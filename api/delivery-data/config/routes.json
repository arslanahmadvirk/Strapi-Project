{
  "routes": [
    {
      "method": "GET",
      "path": "/delivery-data",
      "handler": "delivery-data.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/delivery-data/count",
      "handler": "delivery-data.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/delivery-data/:id",
      "handler": "delivery-data.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/delivery-data",
      "handler": "delivery-data.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/delivery-data/:id",
      "handler": "delivery-data.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/delivery-data/:id",
      "handler": "delivery-data.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
